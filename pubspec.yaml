name: learnx
description: "A new Flutter project. LearnX"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.9.1
  http: ^1.4.0
  shared_preferences: ^2.5.3
  google_fonts: ^6.1.0
  google_sign_in: ^6.2.2
  auth_buttons: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true