import 'package:flutter/material.dart';

class VisaDetail extends StatelessWidget {
  const VisaDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Wisa Detail'),
      ),
      body: Center(
        child: Text('Wisa Detail'),
      ),
    );
  }
}